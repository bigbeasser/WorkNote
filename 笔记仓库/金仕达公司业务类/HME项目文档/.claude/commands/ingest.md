请按照知识库 schema 执行文档摄入流程。

## 启动步骤（必须先执行）

1. 读取 `schema.md` 第 4 节（摄入流程）
2. 读取 `wiki/_MAP.md` 了解当前知识体系
3. 读取 `shturl.md` 前 20 行了解最近变更

## 摄入资料

以下是我要摄入的新资料：

$ARGUMENTS

## 执行要求

按 schema.md 第 4 节的完整流程执行：

1. **阅读与分类**：识别文档类型，决定存放目录和文件名
2. **生成文档**：按 schema.md 第 7 节对应的模板结构生成文档，填写完整 frontmatter
3. **建立关联**：在 frontmatter `related` 中关联相关文档，在已有文档中补充反向链接
4. **更新导航层**：更新对应的 `wiki/<领域>.md` 和 `wiki/_MAP.md`
5. **记录变更**：在 `shturl.md` 顶部追加摄入记录

完成后输出：
- 新建/修改的文件清单
- 建立的链接关系
